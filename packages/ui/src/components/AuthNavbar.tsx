import Logo from "./Logo";

const AuthNavbar = () => {
  return (
    <nav className="flex items-center p-4 bg-card max-sm:justify-center">
      <Logo />
    </nav>
  );
};

export default AuthNavbar;
